module.exports = {
    preset: 'ts-jest',
    testEnvironment: 'node',
    collectCoverage: true,  // Enable coverage reporting (optional)
    coverageDirectory: './coverage',  // Optional: specify where to store coverage reports
};